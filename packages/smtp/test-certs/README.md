SMTP test certificate
=====================

These files are public test fixtures for a TLS-enabled local Mailpit instance.
The private key is intentionally committed and must never be used outside the
test environment.
